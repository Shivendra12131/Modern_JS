// dom is created by the browser when an html document is loadedd inside 
//the browser creates an object which models the document which is called document

// const para=document.getElementById('p');
// const para=document.querySelector('div.error');
// const para=document.querySelectorAll('p');
// para.forEach(para=>{
//     console.log(para);
// })
// const errors=document.querySelectorAll('.error');

// console.log(errors);


// console.log(para);


//query selectpr takes first single element with giving element name in selector 
//query selector takes all elements with that class name giving with .classname in selector 

//get element by id

// document.getElementById('page-title');//will give that element with id page-title

// //get elements by class name
// const errors=document.getElementsByClassName('error');

// console.log(errors);

// node list and html collection in html collection cant use for each with but nodelist can use foreach

//get element by their tag name

// const paras=document.getElementsByTagName('p');// gives the elements with this tags... all elements


// console.log(paras);
// console.log(paras[1]);
//tags and classname gives html collection so we have to frist convert it into arrays 



